<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MiPrimeraApp.MainPage">
 
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="20"
            VerticalOptions="Center">
 
            <Label
            Text="Selecciona una fruta"
            FontSize="24"
            HorizontalOptions="Center" />
 
            <CollectionView
                x:Name="frutasCollectionView"
                SelectionMode="Single"
                SelectionChanged="OnFrutaSeleccionada">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="15" Margin="0,5">
                            <Label Text="{Binding Nombre}" FontSize="18" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- <Label
                Text="¡Bienvenido!"
                FontSize="28"
                HorizontalOptions="Center" />
 
            <Label
                x:Name="saludoLabel"
                Text="Escribe tu nombre y presiona el botón"
                FontSize="16"
                HorizontalOptions="Center" />

            <Entry
                x:Name="nombreEntry"
                Placeholder="Escribe tu nombre aquí"
                FontSize="16" />
            <Button
                x:Name="saludarBtn"
                Text="Saludar"
                Clicked="OnSaludarClicked"
                HorizontalOptions="Center" />    
            <Button
                x:Name="colorBtn"
                Text="Cambiar color"
                Clicked="OnColorClicked"
                HorizontalOptions="Center" />
            <Slider x:Name="tamanoSlider" Minimum="10" Maximum="40" ValueChanged="OnTamanoChanged" /> -->
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
